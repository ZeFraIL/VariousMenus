<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VariousMenus" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="MyPopupMenuStyle" parent="Widget.AppCompat.PopupMenu">
        <item name="android:popupBackground">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">16sp</item>
        <item name="android:showAsAction">always</item>
    </style>

    <style name="Theme.VariousMenus" parent="Base.Theme.VariousMenus" >
        <item name="android:popupMenuStyle">@style/MyPopupMenuStyle</item>
    </style>
</resources>